<p>
  <%= t('user_journey.find_out_more', 
    link: link_to(t('user_journey.rotate_key_and_certificate', 
      component: @certificate.component.display, 
      certificate: certificate.usage), t("user_journey.urls.#{@certificate.component.type}.#{certificate.usage}"))
    ).html_safe 
  %>
</p>